1. generate a note for new youtube video and make clause update the prompt in "prompt/youtube-link-to-notes.prompt.md" so that it saves some effort and sredit and uses same technique to get transcripts faster